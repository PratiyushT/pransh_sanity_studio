import product from "./product";
import category from "./category";
import color from "./color";
import size from "./size";
import variants from "./variants";

export const schemaTypes = [product, category, color, size, variants]
